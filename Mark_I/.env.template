# GitHub configuration for MCP Security Scanner
# Set your GitHub personal access token here
GITHUB_TOKEN=your_github_token_here

# Webhook secret (generate a random secret)
WEBHOOK_SECRET=your_webhook_secret_here

# Multi-repository monitoring configuration
# Monitor all repositories from these GitHub users (comma-separated)
MONITORED_USERS=your_github_username

# Monitor all repositories from these GitHub organizations (comma-separated)
MONITORED_ORGS=

# Only monitor these specific repositories (comma-separated, format: owner/repo)
# If set, ONLY these repos will be monitored (overrides other settings)
ALLOWED_REPOS=

# Ignore these repositories (comma-separated, format: owner/repo)
IGNORED_REPOS=

# Instructions:
# 1. Go to https://github.com/settings/tokens
# 2. Create a new token with 'repo' scope
# 3. Replace 'your_github_token_here' with your actual token
# 4. Replace 'your_webhook_secret_here' with a random secret (or use setup_github.py)
# 5. Replace 'your_github_username' with your GitHub username
# 6. Configure monitoring scope using the variables above
